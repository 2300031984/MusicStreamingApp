apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: music-app
  labels:
    app: frontend
spec:
  type: LoadBalancer  # Change to ClusterIP if using Ingress
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend

